#include "main.h"
/**
 * print_rev - Prints a string in reverse
 * @s: string
 * Return: void
 */
void print_rev(char *s)
{
for (int i = 15; i >= 0 ; i--)
{
_putchar(s[i]);
}
    _putchar('\n');
    }
