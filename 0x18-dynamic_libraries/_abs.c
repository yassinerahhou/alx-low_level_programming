#include "main.h"
/**
 * _abs - computes the absolute valu dhdhdfy dhud
 * @n: the integer to compute ghgsgsyt yyys
 * Return: the absolute value of hshsyg ggdgd
 */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
