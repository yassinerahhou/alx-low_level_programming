#include "main.h"
/**
 * _puts - prints a string uièi_o
 * @str: pointer to the strinçs dhjd
*/
void _puts(char *str)
{
int i;
for (i = 0; str[i] != '\0'; i++)
{
_putchar(str[i]);
}
_putchar('\n');
}
